//#-hidden-code
import PlaygroundSupport
import Foundation

let ev3 = PlaygroundPageSupport.createRobot()
//#-end-hidden-code
/*:#localized(key: "FirstProseBlock")
  ### Program

  Using the turtle prototype you have just built, create a function named `moveTurtle` that:

  1.  Moves your turtle forward for a distance of 40 cm and stops at the "fruit" without using a sensor.

  -   example: To control motors, remember the `motorOn` function that was covered in the previous chapters. To control two motors simultaneously, you can explore the `move` function. For example:\
      \
      `move(forRotations: 1.0, leftPort: .b, rightPort: .c, leftPower: 100.0, rightPower: 100.0)`

  ### Analyze

  Use the information found in Port View and Data View to determine the speed of your turtle.
  */   
//#-code-completion(everything, hide)
//#-code-completion(currentmodule, show)
//#-code-completion(literal, show, integer)
//#-code-completion(keyword, show, func, while, if)
//#-code-completion(identifier, show, ev3, .)
//#-code-completion(identifier, show, move(forSeconds:leftPort:rightPort:leftPower:rightPower:))
//#-code-completion(identifier, show, OutputPort, a, b, c, d)
//#-code-completion(identifier, show, move(forDegrees:leftPort:rightPort:leftPower:rightPower:))
//#-code-completion(identifier, show, move(forRotations:leftPort:rightPort:leftPower:rightPower:))
//#-editable-code
//#-end-editable-code
